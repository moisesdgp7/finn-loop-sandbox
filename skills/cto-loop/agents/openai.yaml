interface:
  display_name: "CTO Loop"
  short_description: "Repeat bounded CTO Build or Review passes"
  default_prompt: "Use $cto-loop in build or review mode with an explicit pass limit or stop condition."
